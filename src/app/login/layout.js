export const metadata = {
  title: "Access Student Command Cockpit — PhysicsVault Login",
  description: "Log into your PhysicsVault account to access advanced JEE/NEET simulators, study analytics, and AI tutor models.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
