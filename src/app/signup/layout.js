export const metadata = {
  title: "Join the PhysicsVault Fleet — Student Registration",
  description: "Register for a PhysicsVault account to unlock elite cosmic workspace panels, dynamic note libraries, and step-by-step AI scientific solvers.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
